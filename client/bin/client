// #!/usr/local/bin/node
const workingDirectory = '../tmp'
const storage require('node-persist').initSync()
const identities = ['file-server','directory-server']

const cli = require('commander')
cli.version('0.0.1')

cli
.command('login <username> <username>')
.action(function(username, password){
})

cli
.command('open <file> in <mode>')
.action(function(file, mode){
})

cli
.command('close <file> in <mode>')
.action(function(file, mode){
})

cli.parse(process.argv)
